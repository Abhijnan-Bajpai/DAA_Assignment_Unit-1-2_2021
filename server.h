//returns a random array of specified size
void randArr(int*, int);
//swap two elements
void swap(int*, int*);

//Select an element for sorting
int pick(int*, int);
//Perform selection sort
void selectionSort(int*, int);

//Perform bubble sort
void bubbleSort(int*, int);
//One iteration of bubble
void bubble(int*, int, int);

//Driver function for quicksort
void quickSort(int*, int, int);
//Partitioning of quicksort
int partition(int*, int, int);

//Merging function for mergesort
void merge(int*, int, int, int);
//Driver function for mergesort
void mergeSort(int*, int, int);

//Returns the comparison count
long long int countComparisons(int*, int, int);
//Returns the time count
int countTime(int*, int);

//Function for writing the file
void writeFile(int, FILE*, FILE*);



















